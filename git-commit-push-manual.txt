# קוד להדבקה בטרמינל – קומיט ופוש ידני
# העתק רק את השורות שמתחת לקו (ללא #)

# --- הדבק את זה בטרמינל ---

cd "c:\Users\User\Downloads\אתר בינה מלאכותית ללימוד"
git add -A
git status
git commit -m "עדכון: סדר שיעורים, היסטוריה ומילון לפי סדר כרונולוגי"
git push

# --- סיום ---

# אם תרצה להוסיף הודעה משלך, החלף את הטקסט במרכאות אחרי -m
# דוגמה: git commit -m "התיקונים שלי"
